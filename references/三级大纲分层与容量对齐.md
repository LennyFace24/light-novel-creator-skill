# 三级大纲 + 扩容优先级

一级(固定)：主线总纲
二级(可控)：卷3-5节点 — 主线不变下可新增/拆分
三级(灵活)：单话大纲 — 完全开放修改

容量：节拍=话数×3

## 扩容优先级
P1 节点内插入：现有节点间加1-2事件（最安全）
P2 节点展开分支：简要情节展开为完整事件
P3 改接口：本话结尾后移，末尾加新事件，同步更新下话大纲

## 边界约束
向上兼容不冲突 / 改接口须向下通知 / 禁止回改已锁定 / 禁止以大纲已确定拒绝扩写
